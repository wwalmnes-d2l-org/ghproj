---
# ghproj https://github.com/suzuki-shunsuke/ghproj
entries:
  - query: |
      is:open
      -project:wwalmnes-d2l-org/2
      archived:false
      owner:wwalmnes-d2l-org
  # owner:aquaproj
  # owner:lintnet
  # is:public
    expr: |
      (! Item.Repo.IsFork) &&
      (Item.Title != "Dependency Dashboard") &&
      ! (Item.Repo.Name startsWith "homebrew-") &&
      ! (Item.Repo.Name startsWith "test-") &&
      ! (Item.Repo.Name startsWith "ghproj")
    project_id: PVT_kwDOEA845s4BSTph
    # type: issue, pr
    # title
    # label
    # repository name
    # repository properties
    # state: open, closed
    # merged: true/false
    # datetime created, updated, closed, merged
    # OR
  # - expr: |
  #     Item.Repo.IsArchived ||
  #     (Item.Title == "Dependency Dashboard") ||
  #     Item.Repo.IsFork ||
  #     (Item.Repo.Name startsWith "homebrew-") ||
  #     (Item.Repo.Name startsWith "test-")
  # - expr: |
  #     Item.Repo.IsArchived
  #   action: archive
  #   project_id: PVT_kwHOAMtMJ84AQCf4
